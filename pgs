player = Player {}

keyboard.space.onclick = function() {
	player.physics.velocity.y = 10
}

floor = Object {}
floor.collisionShape = Plane {
	width: 100
	height: 100
}
floor.collision_mask = 
floor.translation = Vec3(0, -5, 0)

player.collides(floor, () => {
	
})